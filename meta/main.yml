---
galaxy_info:
  role_name: nginx_certbot
  author: chadek
  description: ansible role to install and setup nginx with https server templates using certbot for linux
  company: Green-house
  license: license GPL-3.0-only
  min_ansible_version: "2.4"
  galaxy_tags:
    - nginx
    - certbot
    - https
    - ssl

dependencies:
  []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
